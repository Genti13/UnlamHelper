import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.genti.unlamHelper',
  appName: 'unlamHelper',
  webDir: 'dist/unlamHelper/browser'
};

export default config;
